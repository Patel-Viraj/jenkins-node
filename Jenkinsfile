pipeline {
         agent any
         stages {
                 stage('test') {
                  steps {
                     echo 'staring testing the app'
                  //    sh 'npm i'
                  }
                 }
                 stage('build') {
                  steps {
                        echo "App is building"
                        // sh 'npm build-dev'
                   }
                 }
             
      }
}
